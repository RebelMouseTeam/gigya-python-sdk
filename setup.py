from setuptools import setup, find_packages


setup(
    name = 'gigya',
    version = '3.3.5',
    url = '',
    description = 'Gigya Python SDK',
    package_dir = {'': '.'},
    packages = find_packages()
)